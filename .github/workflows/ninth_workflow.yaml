name: Ninth workflow
on: 
  repository_dispatch: 
    types: [test]

jobs: 
  run-first-command:
    runs-on: ubuntu-latest
    steps:
        - name: Event vars
          run: echo ${{github.event.client_payload.titleTest}}"
