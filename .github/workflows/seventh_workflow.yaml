name: Seventh workflow
on: 
  schedule:
    - cron:  '*/5 * * * *'     

jobs: 
  success-command:
    runs-on: ubuntu-latest
    steps:
        - name: Event vars
          run: |
            echo $GITHUB_SHA
            echo $GITHUB_REF
