name: Fifth workflow
on: [push, issues, pull_request]

jobs: 
  success-command:
    runs-on: ubuntu-latest
    steps:
        - name: Event vars
          run: |
            echo $GITHUB_SHA
            echo $GITHUB_REF
